# Table assets

CSV tables are tracked as plain text for review. The XLSX workbook path is reserved as a Git LFS pointer placeholder; replace it with the final workbook through Git LFS before production publication.
