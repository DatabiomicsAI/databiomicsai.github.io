# Supplementary assets

Plain-text supplementary manifests are tracked in Git. The ZIP submission package path is reserved as a Git LFS pointer placeholder; replace it with the final package through Git LFS before production publication.
